
# Genkit / Gemini API Key (if using AI features)
GOOGLE_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Admin Panel Credentials (must be prefixed with NEXT_PUBLIC_ to be available client-side)
NEXT_PUBLIC_ADMIN_EMAIL=admin@ecoinvent.in
NEXT_PUBLIC_ADMIN_PASSWORD=password

# Note: For local development, copy this file to .env.local and fill in your actual values.
# .env.local is ignored by Git by default.
